import cardControllers from './card';
import userControllers from './user';
import authControllers from './auth';

const controllers = {
  card: cardControllers,
  user: userControllers,
  auth: authControllers,
};

export default controllers;
